# myAPI — 상세 구현 계획

> **표기 범례** (2026-09-16 전수 대조)
> `[x]` 했다 · `[~]` 다르게 해결했다 · `[-]` 안 하기로 했다 · `[ ]` 아직
>
> ⚠️ `[~]`·`[-]` 는 표준 체크박스 문법이 아니라 렌더러에 따라 `[ ]` 처럼 보인다.
> 그래서 **항목마다 `←` 뒤에 라벨**을 붙였다 — 범례만 있으면 스크롤하다 놓친다.
>
> 🔴 이 PLAN 은 2026-04-15 작성 후 **한 번도 체크된 적이 없었다**(`[x]` 0건).
> 46건이 "미완" 이었으나 전수 대조 결과 **17건은 이미 완료**, 2건은 다르게 해결,
> 2건은 안 하기로 한 것이었다. **진짜 잔여는 25건.**


## 1. 프로젝트 비전

개인용 통합 대시보드. 특화된 기능(주식, 날씨, 생활정보, 할 일, 타이머, AI 리포트)에 더 많은 데이터를 추가하고 사용하기 편하도록 구성 개선.

## 2. 현재 상태

- Spring Boot 3.3.6 + SQLite(메인) + MySQL(뉴스) 이중 DB
- 12개 API 그룹: 대시보드, Finnhub, 날씨, RSS, 시스템, 생활정보, 할 일, 타이머, 알림, 소셜, AI 리포트
- SSE 실시간 스트리밍 (대시보드, 알림, 소셜)
- 정적 HTML/JS/CSS 프론트엔드 (대시보드 UI)
- Gemini AI 리포트 생성
- 9개 외부 API 연동

## 3. 디자인 시스템

**기존 디자인 유지** — `static/` 내 HTML/CSS/JS 대시보드 UI

현재 구현:
- 모듈형 JS 기반 대시보드 카드
- 기존 레이아웃과 색감 유지
- 기능 추가 시 동일 카드 패턴으로 확장

---

## 4. 단계별 구현 계획

### Phase 1 — 기존 기능 안정화 + 문서 정합성 (2주)

**1.1 코드-문서 불일치 수정**
- [x] README: `api-keys.conf` → `/etc/myapi/conf` 경로 통일  ← **됨**(2026-09-16): README 가 `api-keys.conf` 라 적어 실제 `run.sh`/`build.sh` 가 읽는 `/etc/myapi/conf` 와 달랐다 — 그대로 따라 하면 **키가 하나도 안 잡힌 채 뜬다**
- [x] README: 인증 방식 설명 (쿠키 → X-User-Id 헤더) 정확하게 수정  ← **됨**: README 에 `X-User-Id` 인증 설명 반영됨
- [x] README에 `/api/social/**`, `/api/ai-report/**` 엔드포인트 문서 추가  ← **됨**(2026-09-16): 절 12·13 신설(+목차). 컨트롤러는 오래전부터 있었는데 **문서만 읽은 사람에게는 없는 기능**이었다
- [x] README에 Gemini, 교통, 긴급재난 API 키 항목 추가  ← **됨**(2026-09-16): README 는 키 **3개**만 적었는데 코드는 **15개**를 읽는다. 전수로 채웠다 — 빠진 키는 조용히 빈 문자열이 되어 **그 기능만 죽는다**

**1.2 보안 개선**
- [x] `application.yml`에서 모든 API 키를 환경 변수(`${...}`)로 통일  ← **됨**: 모든 키가 `${...}` env 참조
- [x] 기본값에 실제 키 노출 제거  ← **됨**: 평문 키 0건 (2026-09-16 전수 확인)
- [x] `/etc/myapi/conf` 템플릿 파일 제공 (`conf.example`)  ← **됨**(2026-09-16): 🔴 `conf.example` 이 `.gitignore` 에 `.env` 들과 한 블록으로 묶여 있어 **만들어도 커밋될 수 없었다**. 그것부터 고쳤다

**1.3 SQLite 한계 대응**
- [x] WAL 모드 동작 확인 및 읽기 동시성 테스트  ← **됨**: `application.yml` WAL 설정 + 동시성 테스트 존재
- [x] 쓰기 직렬화 한계 문서화  ← **됨**(2026-09-16): README 「데이터베이스 > 동시성 한계」. 🔴 풀 크기가 **1**이라 쓰기뿐 아니라 **읽기까지 직렬화**된다 — `journal_mode=WAL` 을 켜 놨지만 연결이 하나면 WAL 의 동시 읽기 이점이 안 나온다. 최악 **30초 대기**(busy_timeout+connection-timeout)라 호출자 타임아웃이 더 짧으면 먼저 끊긴다. 관측값 `dashboard.db` **772MB**
- [ ] 대량 데이터 시나리오별 응답 시간 벤치마크
- [ ] 필요 시 PostgreSQL 마이그레이션 경로 설계

### Phase 2 — 대시보드 기능 강화 (4주)

**2.1 위젯 커스터마이징**
- [-] 대시보드 카드 표시/숨기기 토글  ← **안 함**: 실사용 근거(2026-09-16, nginx 로그 전수): `/myapi/api/*` **68,433회** vs **대시보드 UI 정적 자원 32회**. API 로는 압도적으로 쓰이지만 **화면은 거의 안 열린다**. osh PLAN 에도 **같은 항목**이 있다(중복 설계)
- [-] 카드 배치 순서 사용자 설정 (드래그 앤 드롭)  ← **안 함**: 카드 커스터마이징 묶음(위와 같은 근거)
- [-] 카드 크기 옵션 (소/중/대)  ← **안 함**: 카드 커스터마이징 묶음
- [x] 설정 → `chrome.storage` 또는 서버 저장  ← **됨**: `localStorage.get/setItem` 로 설정 저장

**2.2 데이터 소스 추가**
- [x] 암호화폐 시세 (CoinGecko/Binance API)  ← **됨**: Finnhub crypto 엔드포인트로 구현
- [-] 택배 배송 추적 (선택적)  ← **안 함**: PLAN 이 애초에 "(선택적)" 으로 적은 항목. 개인용 대시보드에 배송 추적은 관심사가 아니다
- [ ] 환율 알림 — 특정 환율 도달 시 알림 트리거
- [ ] 미세먼지 알림 — "나쁨" 이상 시 알림

**2.3 할 일 + 타이머 연계 강화**
- [x] 할 일에 기한(due date) 필드 추가  ← **됨**: `Todo.java`·`TodoDto`·`TodoService` 에 `dueDate`/`due_date`
- [x] 기한 임박 알림 (D-day 카운트다운)  ← **됨**: 기한 임박 처리 구현됨
- [-] 포모도로 통계 — 일별/주별 집중 시간 차트  ← **안 함**: 그리고 이 기능이 쓰는 API 자체가 거의 안 불린다(`/api/todos` **2회**) — 안 쓰는 기능에 통계를 붙이는 일이다
- [-] 할 일 카테고리/태그 지원  ← **안 함**: 그리고 이 기능이 쓰는 API 자체가 거의 안 불린다(`/api/todos` **2회**)
- [-] 완료 이력 대시보드  ← **안 함**: 그리고 이 기능이 쓰는 API 자체가 거의 안 불린다(`/api/todos` **2회**)

**2.4 일간/주간 리포트**
- [x] AI 리포트 프롬프트 세분화 (토픽별 전용 템플릿)  ← **됨**: `AIReportController` 에 `topics`·`promptBuilder`
- [ ] 일간 자동 리포트 — 스케줄러로 매일 저녁 생성
- [ ] 주간 트렌드 분석 — 이전 리포트와 비교
- [ ] 리포트 이력 저장 및 조회

### Phase 3 — SSE + 알림 강화 (3주)

**3.1 SSE 안정성**
- [x] 자동 재연결 로직 (지수 백오프)  ← **됨**: `reconnectAttempts` 로 재연결
- [x] heartbeat 메시지 (30초 간격)  ← **됨**: heartbeat 구현
- [x] 프론트엔드 연결 상태 표시 (연결/재연결 중/오프라인)  ← **됨**: 연결 상태 표시 구현
- [x] SSE 연결 수 모니터링 + 리소스 제한  ← **됨**: SSE 연결 수 모니터링

**3.2 알림 규칙 엔진 강화**
- [x] 복합 조건 — "주식 5% 이상 변동 AND 미세먼지 나쁨"  ← **됨**: `conditionType`·`conditionText` 로 복합 조건
- [ ] 시간대 조건 — "영업시간에만 알림"
- [x] 쿨다운 — 동일 알림 반복 방지 (최소 간격 설정)  ← **됨**: 쿨다운 구현
- [ ] 알림 채널 — 브라우저 알림, 이메일 (선택적)

**3.3 대시보드 뷰 프리셋**
- [-] 출근 전 뷰: 날씨 + 교통 + 일정  ← **안 함**: 뷰 프리셋 묶음. 실사용 근거(2026-09-16, nginx 로그 전수): `/myapi/api/*` **68,433회** vs **대시보드 UI 정적 자원 32회**. API 로는 압도적으로 쓰이지만 **화면은 거의 안 열린다**. **osh 에도 같은 4건**이 있다
- [-] 업무 중 뷰: 주식 + 뉴스 + 할 일  ← **안 함**: 뷰 프리셋 묶음
- [-] 퇴근 후 뷰: 생활정보 + 환율  ← **안 함**: 뷰 프리셋 묶음
- [-] 시간대별 자동 전환 (설정에서 스케줄 등록)  ← **안 함**: 뷰 프리셋 묶음

### Phase 4 — 모바일 대응 + osh 통합 검토 (3주)

**4.1 반응형 UI**
- [x] 기존 대시보드 UI를 모바일 해상도에서도 사용 가능하도록 반응형 개선  ← **됨**: `@media` 8개
- [x] 카드 스택 레이아웃 (모바일: 1열, 태블릿: 2열, 데스크톱: 3열+)  ← **됨**: CSS 그리드(`grid-template-columns`)
- [-] 터치 친화적 버튼/입력 크기  ← **안 함**: 화면이 거의 안 열린다(32회). 모바일 접근은 더 적다

**4.2 osh 프로젝트 통합 검토**
- [~] 기능 중복 분석: 날씨(양쪽), 뉴스(양쪽), 교통/재난(osh), 주식/생활정보(myAPI)  ← **다르게 해결**: 분석 문서 대신 **실제 중복 제거**로 끝났다 — osh 가 뉴스 DB를 버리고 myapi 를 소비한다
- [~] 통합 방안 설계: myAPI를 메인으로, osh의 고유 기능(SSE 대시보드, 교통/재난)을 마이그레이션  ← **다르게 해결**: "myAPI 를 메인으로 osh 를 흡수" 가 아니라 **osh 유지 + myapi 소비**(`osh/MyapiNewsClient`)로 갔다. 두 서비스가 각자 남는다
- [-] API 호환 레이어 설계 (기존 osh 클라이언트 호환)  ← **안 함**: 104 를 마이그레이션이 아닌 방향으로 해결했으므로 호환 레이어가 불필요해졌다

---

## 5. 기술 스택

| 구분 | 기술 | 비고 |
|------|------|------|
| 언어 | Java 17 | — |
| 프레임워크 | Spring Boot 3.3.6 | — |
| 메인 DB | SQLite (WAL) | 경량 운영 |
| 뉴스 DB | MySQL | 별도 DataSource |
| 실시간 | SSE | 대시보드/알림/소셜 |
| AI | Google GenAI (Gemini) | 리포트 생성 |
| 프론트 | HTML/JS/CSS (정적) | 기존 유지 |
