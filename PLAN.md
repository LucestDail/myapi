# myAPI — 상세 구현 계획

## 1. 프로젝트 비전

개인용 통합 대시보드. 특화된 기능(주식, 날씨, 생활정보, 할 일, 타이머, AI 리포트)에 더 많은 데이터를 추가하고 사용하기 편하도록 구성 개선.

## 2. 현재 상태

- Spring Boot 3.3.6 + SQLite(메인) + MySQL(뉴스) 이중 DB
- 12개 API 그룹: 대시보드, Finnhub, 날씨, RSS, 시스템, 생활정보, 할 일, 타이머, 알림, 소셜, AI 리포트
- SSE 실시간 스트리밍 (대시보드, 알림, 소셜)
- 정적 HTML/JS/CSS 프론트엔드 (대시보드 UI)
- Gemini AI 리포트 생성
- 9개 외부 API 연동

## 3. 디자인 시스템

**기존 디자인 유지** — `static/` 내 HTML/CSS/JS 대시보드 UI

현재 구현:
- 모듈형 JS 기반 대시보드 카드
- 기존 레이아웃과 색감 유지
- 기능 추가 시 동일 카드 패턴으로 확장

---

## 4. 단계별 구현 계획

### Phase 1 — 기존 기능 안정화 + 문서 정합성 (2주)

**1.1 코드-문서 불일치 수정**
- [ ] README: `api-keys.conf` → `/etc/myapi/conf` 경로 통일
- [ ] README: 인증 방식 설명 (쿠키 → X-User-Id 헤더) 정확하게 수정
- [ ] README에 `/api/social/**`, `/api/ai-report/**` 엔드포인트 문서 추가
- [ ] README에 Gemini, 교통, 긴급재난 API 키 항목 추가

**1.2 보안 개선**
- [ ] `application.yml`에서 모든 API 키를 환경 변수(`${...}`)로 통일
- [ ] 기본값에 실제 키 노출 제거
- [ ] `/etc/myapi/conf` 템플릿 파일 제공 (`conf.example`)

**1.3 SQLite 한계 대응**
- [ ] WAL 모드 동작 확인 및 읽기 동시성 테스트
- [ ] 쓰기 직렬화 한계 문서화
- [ ] 대량 데이터 시나리오별 응답 시간 벤치마크
- [ ] 필요 시 PostgreSQL 마이그레이션 경로 설계

### Phase 2 — 대시보드 기능 강화 (4주)

**2.1 위젯 커스터마이징**
- [ ] 대시보드 카드 표시/숨기기 토글
- [ ] 카드 배치 순서 사용자 설정 (드래그 앤 드롭)
- [ ] 카드 크기 옵션 (소/중/대)
- [ ] 설정 → `chrome.storage` 또는 서버 저장

**2.2 데이터 소스 추가**
- [ ] 암호화폐 시세 (CoinGecko/Binance API)
- [ ] 택배 배송 추적 (선택적)
- [ ] 환율 알림 — 특정 환율 도달 시 알림 트리거
- [ ] 미세먼지 알림 — "나쁨" 이상 시 알림

**2.3 할 일 + 타이머 연계 강화**
- [ ] 할 일에 기한(due date) 필드 추가
- [ ] 기한 임박 알림 (D-day 카운트다운)
- [ ] 포모도로 통계 — 일별/주별 집중 시간 차트
- [ ] 할 일 카테고리/태그 지원
- [ ] 완료 이력 대시보드

**2.4 일간/주간 리포트**
- [ ] AI 리포트 프롬프트 세분화 (토픽별 전용 템플릿)
- [ ] 일간 자동 리포트 — 스케줄러로 매일 저녁 생성
- [ ] 주간 트렌드 분석 — 이전 리포트와 비교
- [ ] 리포트 이력 저장 및 조회

### Phase 3 — SSE + 알림 강화 (3주)

**3.1 SSE 안정성**
- [ ] 자동 재연결 로직 (지수 백오프)
- [ ] heartbeat 메시지 (30초 간격)
- [ ] 프론트엔드 연결 상태 표시 (연결/재연결 중/오프라인)
- [ ] SSE 연결 수 모니터링 + 리소스 제한

**3.2 알림 규칙 엔진 강화**
- [ ] 복합 조건 — "주식 5% 이상 변동 AND 미세먼지 나쁨"
- [ ] 시간대 조건 — "영업시간에만 알림"
- [ ] 쿨다운 — 동일 알림 반복 방지 (최소 간격 설정)
- [ ] 알림 채널 — 브라우저 알림, 이메일 (선택적)

**3.3 대시보드 뷰 프리셋**
- [ ] 출근 전 뷰: 날씨 + 교통 + 일정
- [ ] 업무 중 뷰: 주식 + 뉴스 + 할 일
- [ ] 퇴근 후 뷰: 생활정보 + 환율
- [ ] 시간대별 자동 전환 (설정에서 스케줄 등록)

### Phase 4 — 모바일 대응 + osh 통합 검토 (3주)

**4.1 반응형 UI**
- [ ] 기존 대시보드 UI를 모바일 해상도에서도 사용 가능하도록 반응형 개선
- [ ] 카드 스택 레이아웃 (모바일: 1열, 태블릿: 2열, 데스크톱: 3열+)
- [ ] 터치 친화적 버튼/입력 크기

**4.2 osh 프로젝트 통합 검토**
- [ ] 기능 중복 분석: 날씨(양쪽), 뉴스(양쪽), 교통/재난(osh), 주식/생활정보(myAPI)
- [ ] 통합 방안 설계: myAPI를 메인으로, osh의 고유 기능(SSE 대시보드, 교통/재난)을 마이그레이션
- [ ] API 호환 레이어 설계 (기존 osh 클라이언트 호환)

---

## 5. 기술 스택

| 구분 | 기술 | 비고 |
|------|------|------|
| 언어 | Java 17 | — |
| 프레임워크 | Spring Boot 3.3.6 | — |
| 메인 DB | SQLite (WAL) | 경량 운영 |
| 뉴스 DB | MySQL | 별도 DataSource |
| 실시간 | SSE | 대시보드/알림/소셜 |
| AI | Google GenAI (Gemini) | 리포트 생성 |
| 프론트 | HTML/JS/CSS (정적) | 기존 유지 |
